procs:
  frontend:
    cwd: "./apps/frontend"
    cmd: ["pnpm", "dev"]
    env:
      NODE_ENV: development
    
  # When you add your second app, uncomment and configure:
  # backend:
  #   cwd: "./apps/backend" 
  #   cmd: ["pnpm", "dev"]
  #   env:
  #     NODE_ENV: development
  
  # database:
  #   cwd: "./apps/backend"
  #   cmd: ["pnpm", "db:dev"]

# Global settings
shell: false
hide_keymap_window: false