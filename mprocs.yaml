# CeraUI Development Process Manager
# Environment variables are loaded from root .env via dotenv-cli
# Run with: bun run dev

procs:
  frontend:
    cwd: "./apps/frontend"
    cmd: ["bun", "run", "dev"]

  backend:
    cwd: "./apps/backend"
    cmd: ["bun", "run", "dev"]

  i18n:
    cwd: "./packages/i18n"
    cmd: ["bun", "run", "dev"]

  # database:
  #   cwd: "./apps/backend"
  #   cmd: ["bun", "run", "db:dev"]

# Global settings
shell: false
hide_keymap_window: false
