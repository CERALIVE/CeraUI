{
	"name": "backend",
	"version": "1.0.0",
	"description": "CeraUI backend - Node.js/Bun server for CERALIVE/BELABOX live streaming hardware",
	"type": "module",
	"author": "Andrés Cera",
	"license": "GPL-3.0",
	"dependencies": {
		"finalhandler": "^2.1.0",
		"http-proxy": "^1.18.1",
		"serve-static": "^2.2.0",
		"winston": "^3.18.3",
		"ws": "^8.18.3",
		"xml2js": "^0.6.2"
	},
	"devDependencies": {
		"@types/bun": "^1.3.3",
		"@types/finalhandler": "^1.2.4",
		"@types/http-proxy": "^1.17.17",
		"@types/serve-static": "^2.2.0",
		"@types/ws": "^8.18.1",
		"@types/xml2js": "^0.4.14",
		"npm-run-all2": "^8.0.4",
		"typescript": "^5.9.3"
	},
	"scripts": {
		"dev": "NODE_ENV=development bun run src/belaUI.ts",
		"dev:single-modem": "NODE_ENV=development MOCK_SCENARIO=single-modem bun run src/belaUI.ts",
		"dev:streaming": "NODE_ENV=development MOCK_SCENARIO=streaming-active bun run src/belaUI.ts",
		"build": "rm -rf ../../dist/ && bash -c 'BUN_TARGET=${BUILD_ARCH:-arm64}; case $BUN_TARGET in amd64) BUN_TARGET=\"bun-linux-x64\" ;; arm64) BUN_TARGET=\"bun-linux-arm64\" ;; *) echo \"Unsupported architecture: $BUN_TARGET\"; exit 1 ;; esac; bun build --compile --minify --sourcemap --bytecode --target=$BUN_TARGET src/belaUI.ts --outfile ../../dist/belaUI' && cd ../.. && pnpm run build:frontend",
		"build:backend-only": "rm -rf ../../dist/belaUI ../../dist/deployment && bash -c 'BUN_TARGET=${BUILD_ARCH:-arm64}; case $BUN_TARGET in amd64) BUN_TARGET=\"bun-linux-x64\" ;; arm64) BUN_TARGET=\"bun-linux-arm64\" ;; *) echo \"Unsupported architecture: $BUN_TARGET\"; exit 1 ;; esac; bun build --compile --minify --sourcemap --bytecode --target=$BUN_TARGET src/belaUI.ts --outfile ../../dist/belaUI'",
		"check": "bun tsc --noEmit",
		"lint": "echo 'Backend linting handled by root Biome'",
		"lint:fix": "echo 'Backend fixing handled by root Biome'",
		"clean": "rm -rf ../../dist/ && rm -rf node_modules"
	},
	"trustedDependencies": [
		"@biomejs/biome"
	]
}
